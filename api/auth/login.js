import {json,method,readBody,sameOrigin} from '../_lib/http.js';import {ensureAuthEnv,sessionCookies,publicUser,responseMessage} from '../_lib/supabase.js';
export default async function handler(req,res){if(req.method!=='POST')return method(res,['POST']);if(!sameOrigin(req))return json(res,403,{error:'Origem da requisição não permitida.'});try{const e=ensureAuthEnv();const b=await readBody(req);const email=typeof b.email==='string'?b.email.trim().toLowerCase():'';const password=typeof b.password==='string'?b.password:'';const captchaToken=typeof b.captchaToken==='string'?b.captchaToken.trim():'';if(!email||!email.includes('@')||password.length<8||!captchaToken)return json(res,400,{error:'Preencha e-mail, senha e captcha.'});const r=await fetch(`${e.url}/auth/v1/token?grant_type=password`,{method:'POST',headers:{apikey:e.publishableKey,'content-type':'application/json'},body:JSON.stringify({email,password,gotrue_meta_security:{captcha_token:captchaToken}}),cache:'no-store'});if(!r.ok)return json(res,r.status===400?401:r.status,{error:await responseMessage(r)});const s=await r.json();if(!s?.access_token||!s?.refresh_token)return json(res,502,{error:'O Supabase não retornou uma sessão válida.'});res.setHeader('Set-Cookie',sessionCookies(s));return json(res,200,{user:publicUser(s.user)});}catch(e){return json(res,500,{error:e instanceof Error?e.message:'Não foi possível entrar.'});}}
