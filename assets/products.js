(async()=>{const S=window.LexusStore;const grid=document.getElementById('products-grid');try{const p=await S.getCatalog();const discount=Math.round((1-p.promotionalPriceCents/p.originalPriceCents)*100);grid.innerHTML=p.variants.map(v=>`<article class="produto-card" data-v="${v.name}"><div class="produto-img"><span class="badge-offer">-${discount}%</span><img src="${v.images[0].src}" alt="${v.images[0].alt}"></div><h3>${p.shortName} — ${v.name}</h3><div class="produto-rating"><span class="stars">★★★★★</span><span>${p.specifications[0].value}</span></div><span class="price-old">${S.money(p.originalPriceCents)}</span><strong class="price-new">${S.money(p.promotionalPriceCents)}</strong><span class="parcelas">Valor oficial do catálogo</span></article>`).join('');grid.querySelectorAll('[data-v]').forEach(x=>x.onclick=()=>location.href=`/produto?cor=${encodeURIComponent(x.dataset.v)}`)}catch(e){grid.innerHTML=`<div class="panel">${e.message}</div>`}})();
